{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "git submodule update --init --depth 1 --recursive >/dev/null 2>&1 || true"
          }
        ]
      }
    ]
  }
}
